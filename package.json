{
  "name": "pricing_fe",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["pricing-dashboard"],
  "scripts": {
    "build": "cd pricing-dashboard && npm run build",
    "dev": "cd pricing-dashboard && npm run dev",
    "start": "cd pricing-dashboard && npm run start",
    "vercel-build": "npm install --legacy-peer-deps && cd pricing-dashboard && npm run build"
  },
  "dependencies": {
    "next": "15.2.4",
    "date-fns": "^3.0.0",
    "react-day-picker": "8.10.1"
  },
  "resolutions": {
    "date-fns": "^3.0.0"
  },
  "overrides": {
    "date-fns": "^3.0.0"
  }
}
